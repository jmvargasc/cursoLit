import { LitElement, html, css } from "lit-element";

export class ViewLUgarescard extends LitElement {
  static styles = css`
    :host {
      display: block;
    }
  `;

  render() {
    return html``;
  }
}
customElements.define("view-Lugarescard", ViewLUgarescard);
